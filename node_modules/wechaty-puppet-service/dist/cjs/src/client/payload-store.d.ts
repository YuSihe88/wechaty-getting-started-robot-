import { ContactPayload, RoomPayload, RoomMemberPayload } from 'wechaty-puppet';
import { FlashStore } from 'flash-store';
interface PayloadStoreOptions {
    token: string;
}
declare class PayloadStore {
    private options;
    contact?: FlashStore<string, ContactPayload>;
    roomMember?: FlashStore<string, RoomMemberPayload>;
    room?: FlashStore<string, RoomPayload>;
    protected storeDir: string;
    protected accountId?: string;
    constructor(options: PayloadStoreOptions);
    /**
     * When starting the store, we need to know the accountId
     *  so that we can save the payloads under a specific account folder.
     */
    start(accountId: string): Promise<void>;
    stop(): Promise<void>;
    destroy(): Promise<void>;
    roomMemberId(roomId: string, memberId: string): string;
}
export { PayloadStore };
//# sourceMappingURL=payload-store.d.ts.map