import { Puppet } from 'wechaty-puppet';
export interface PuppetServerOptions {
    endpoint: string;
    puppet: Puppet;
    token: string;
    tls?: {
        serverCert?: string;
        serverKey?: string;
        disable?: boolean;
    };
}
export declare class PuppetServer {
    readonly options: PuppetServerOptions;
    private grpcServer?;
    constructor(options: PuppetServerOptions);
    version(): string;
    start(): Promise<void>;
    stop(): Promise<void>;
}
//# sourceMappingURL=puppet-server.d.ts.map